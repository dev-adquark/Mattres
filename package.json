{
  "name": "mattress-match-score",
  "version": "0.1.0",
  "private": true,
  "description": "Match Scoring Engine skeleton (v0.1) and static frontend mockups for Mattress Match Score.",
  "main": "src/scoreEngine.js",
  "scripts": {
    "score-demo": "node scripts/score-demo.js",
    "ingest": "node scripts/ingest-mattresses.js",
    "typecheck": "tsc --noEmit",
    "test": "node scripts/test-score-engine.js && node scripts/test-ingest.js && npm run typecheck --silent"
  },
  "engines": {
    "node": ">=14"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
